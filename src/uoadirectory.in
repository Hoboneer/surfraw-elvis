unidirectory.auckland.ac.nz
# Apparently it uses the fragment...
'unidirectory.auckland.ac.nz/search#?'
--description='Search for academic staff, doctoral candidates and a selection of professional staff at the University of Auckland (requires JavaScript)'

--enum=search:all:all,staff,phd
--map=search:category
--metavar=search:category
--describe=search:'Search in CATEGORY'

--enum=sort:relevance:relevance,lastname,firstname
--map=sort:orderBy
# There's probably a better word than "filter" for sort orders.
--metavar=search:filter
--describe=sort:'Order by FILTER'

--enum=results:20:20,50
--map=results:rows
--metavar=results:num
--describe=results:'Number of search results returned'

--query-parameter=search
